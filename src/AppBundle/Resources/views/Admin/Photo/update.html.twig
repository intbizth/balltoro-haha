{% extends 'AppBundle:Admin:layout.html.twig' %}

{% block init %}
    {{ ui_page_options({
        heading: "Photo",
        blocks: {
            content: {
                file: 'AppBundle:Admin/Photo:_form.html.twig',
                params: {photo: photo}
            },
            form: {
                context: form,
                start: {'action': path('route_photo_create')},
                start: {'action': path('route_photo_update', {id: photo.id}), 'method': 'PUT'},
                end: {render_rest: false}
            }
        }
    }) }}
{% endblock %}
