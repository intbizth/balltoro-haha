<a href="{{ url }}">
    {{ label }}
    {% if icon -%}
        {%- if currentOrder == 'desc' -%}
            <i class="fa fa-angle-up"></i>
        {%- else -%}
            <i class="fa fa-angle-down"></i>
        {%- endif %}
    {%- endif %}
</a>
